function addAbout() {
  sessionStorage.about = 1;
}

function noAbout() {
  sessionStorage.about = 0;
}

function addPuzzle() {
  sessionStorage.puzzle = 1;
}

function noPuzzle() {
  sessionStorage.puzzle = 0;
}

function addSerenity() {
  sessionStorage.serenity = 1;
}

function noSerenity() {
  sessionStorage.serenity = 0;
}

function addAcceptance() {
  sessionStorage.acceptance = 1;
}

function noAcceptance() {
  sessionStorage.acceptance = 0;
}

function addApprehension() {
  sessionStorage.apprehension = 1;
}

function noApprehension() {
  sessionStorage.apprehension = 0;
}

function addDistraction() {
  sessionStorage.distraction = 1;
}

function noDistraction() {
  sessionStorage.distraction = 0;
}

function addPensiveness() {
  sessionStorage.pensiveness = 1;
}

function noPensiveness() {
  sessionStorage.pensiveness = 0;
}

function addBoredom() {
  sessionStorage.boredom = 1;
}

function noBoredom() {
  sessionStorage.boredom = 0;
}

function addAnnoyance() {
  sessionStorage.annoyance = 1;
}

function noAnnoyance() {
  sessionStorage.annoyance = 0;
}

function addInterest() {
  sessionStorage.interest = 1;
}

function noInterest() {
  sessionStorage.interest = 0;
}
